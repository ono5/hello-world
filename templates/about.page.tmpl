{{template "base" .}}
{{define "content"}}
    <div class="container">
        <div class="row>
            <div class="col>
                <h1>Tis is the about page</h1>
                <p>Tis is a paragraph of text</p>

                <p>This come from the template: {{index .StringMap "test"}}</p>
            </div>
        </div>
    </div>
{{end}}
